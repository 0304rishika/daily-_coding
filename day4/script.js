  // Basic Arithmetic Operators
  // Add Two Numbers
  // 1.Use the addition operator to calculate the sum of 5 and 10.
  // Find the Remainder
  let a =5;
  let b =10;
  let add =a+b;
  console.log(add);//remainder=15


  // Use the modulus operator to find the remainder when 10 is divided by 3.

  // Comparison Operators
  // Check Equality
  // Use === to check if 5 is equal to '5'.
  let x = 5;
  let y = 5;
  console.log(x === y);

  // Compare Two Numbers
  // Use the > operator to check if 8 is greater than 3.
  let s = 8;
  let r = 3;
  console.log(s>r);

  // Logical Operators
  // Logical AND (&&)
  // Use && to check if true AND false are both true.
  let t =true;
  let i = true;
  console.log(t && i);// answer=true

  // Logical OR (||)
  // Use || to check if either false OR true is true.
  let h = true;
  let l = false;
  console.log(h || l);//answer=true

  // Logical NOT (!)
  // Use ! to invert the value of true
  let n  = 9;
  let p = 10;
  console.log(n != p);

  // Ternary Operator
  // Use the ternary operator to assign yes if 10 > 5, otherwise no.


  // Assignment Operators
  // Compound Assignment
  // Use += to add 5 to the value of a variable.
  let k=7;
  k +=5;
  console.log(k);

  // Subtract Using -=
  // Use -= to subtract 3 from 10.
  let w = 3;
  w -= 10;
  console.log(w);

  // Unary Operators
  // Increment Operator (++)
  // Use the increment operator to increase the value of 5 by 1.
  let o =5;
  o ++;
  console.log(o);

  // Decrement Operator (--)
  // Use the decrement operator to decrease the value of 5 by 1.
  let f=5;
  f --;
  console.log(f);

  // Bitwise Operators
  // Bitwise AND (&)
  // Use the bitwise AND operator with 5 and 3.
  

  // Bitwise OR (|)
  // Use the bitwise OR operator with 5 and 3.

  // Bitwise NOT (~)
  // Use the bitwise NOT operator on 5.

  // Type Operators
  // typeof Operator
  // Use typeof to check the type of 42.